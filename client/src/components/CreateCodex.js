import React, { useContext } from 'react'
import { Button } from 'react-bootstrap'

import { Actions } from '../Actions'

function CreateCodex() {
  const { modals } = useContext(Actions)
  return (

  )
}

export default CreateCodex
